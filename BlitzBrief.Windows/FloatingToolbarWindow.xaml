<Window x:Class="BlitzBrief.Windows.FloatingToolbarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:BlitzBrief.Core.Models;assembly=BlitzBrief.Core"
        Title="BlitzBrief"
        Width="500"
        Height="54"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="Manual">
  <Window.Resources>
    <Style x:Key="ToolbarButton" TargetType="Button">
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="10,0" />
      <Setter Property="FontFamily" Value="Segoe UI" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="#1F2937" />
      <Setter Property="Background" Value="#F8FAFC" />
      <Setter Property="BorderBrush" Value="#D7DEE8" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonChrome"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="16">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonChrome" Property="Background" Value="#EEF2F7" />
                <Setter TargetName="ButtonChrome" Property="BorderBrush" Value="#C5CEDB" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonChrome" Property="Background" Value="#E2E8F0" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PrimaryToolbarButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Background" Value="#111827" />
      <Setter Property="BorderBrush" Value="#111827" />
    </Style>

    <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
      <Setter Property="Width" Value="28" />
      <Setter Property="Height" Value="28" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="FontSize" Value="15" />
      <Setter Property="Foreground" Value="#64748B" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
  </Window.Resources>

  <Border Background="#EFFFFFFF"
          BorderBrush="#BFC7D3"
          BorderThickness="1"
          CornerRadius="18"
          Padding="7"
          MouseLeftButtonDown="Toolbar_MouseLeftButtonDown">
    <Border.Effect>
      <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.18" />
    </Border.Effect>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <UniformGrid Grid.Column="0" Columns="5" Rows="1">
        <Button Content="Diktat"
                Margin="0,0,3,0"
                Style="{StaticResource PrimaryToolbarButton}"
                Tag="{x:Static models:WorkflowType.Transcription}"
                Click="Workflow_Click" />
        <Button Content="Verbessern"
                Margin="3,0,3,0"
                Style="{StaticResource ToolbarButton}"
                Tag="{x:Static models:WorkflowType.TextImprover}"
                Click="Workflow_Click" />
        <Button Content="Easy"
                Margin="3,0,3,0"
                Style="{StaticResource ToolbarButton}"
                ToolTip="Blitzbrief-Easy"
                Tag="{x:Static models:WorkflowType.BlitzBriefEasy}"
                Click="Workflow_Click" />
        <Button Content="Kontext"
                Margin="3,0,3,0"
                Style="{StaticResource ToolbarButton}"
                ToolTip="Blitzbrief-Kontext: setzt den angefangenen Satz links vom Cursor fort"
                Tag="{x:Static models:WorkflowType.BlitzBriefKontext}"
                Click="Workflow_Click" />
        <Button Content="Beruhigen"
                Margin="3,0,0,0"
                Style="{StaticResource ToolbarButton}"
                Tag="{x:Static models:WorkflowType.DampfAblassen}"
                Click="Workflow_Click" />
      </UniformGrid>

      <Button Grid.Column="1"
              Content="×"
              Margin="8,0,0,0"
              Style="{StaticResource CloseButton}"
              ToolTip="Leiste ausblenden"
              Click="Close_Click" />
    </Grid>
  </Border>
</Window>
